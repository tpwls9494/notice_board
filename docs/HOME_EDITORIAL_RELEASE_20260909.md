# 2026-09-09 홈·편집 본문 운영 반영

상태: 운영 전환 및 외부 확인 완료. 원고 신규 공개 없음.

## 범위

- 함께 볼 소식 제목 아래 얇은 구분선, 최신·인기 선택은 별도 테두리/배경 영역.
- 기존 좌우 홈 구조, 대표 글과 목록 분리, 실제 이미지 조건부 표시, 추천·댓글 표시.
- 라운지 상단·홈 홍보 제거 및 기존 주소 보존.
- 선택적 Markdown 본문과 검토함 편집·미리보기.
- 소식·연구의 실습 필수 해제, 활용법의 실행 안내 요구 유지.
- 누적 인기 계산 및 최신 게시순. 최근 6시간 계산 API는 있지만 홈 탭에서는 제외.
- 하단 원문은 제목에 걸린 링크 하나로 간소화.

이전 운영 이미지 대비 변경 배포 소스는 15개다. 블로그 소스·로그인 설정은 변경하지 않았다. 공통 Nginx 이미지에 두 앱이 들어 있어 전환 중 두 사이트를 함께 점검했다.

## 확정 산출물

- 소스 manifest: 342개 파일
- backend: `sha256:bb048bbeb465554ffb77e891ceb69ad67097adf1732a9349b6e5f3bcb8cc47c1`
- nginx: `sha256:7b28bfe796210ee8f06f2a74cc1b473132b3f55968b7384eb6ad7b93de0a1ea0`
- DB: `202609060002` → `202609090001` (선택적 signals.body 추가)
- 실행 명령 전체 경과: 25.39초. 순수 사용자 체감 중단 시간을 별도 측정한 값은 아니다.

현재 운영 경로: `/home/ubuntu/jion-release-20260907T1120/home-editorial-20260909`

Compose 프로젝트: `notice_board`, 파일: `runtime-compose.json`. 런타임 설정·`.env`·백업은 비공개 운영 자료다. 저장소에 업로드하지 않는다.

백업: `backups/release-20260908T180505Z-gPK980`

점검 기록: `backups/preparation-20260908T180458Z-2809831-SgVZkS`

위 시각은 UTC 기반 디렉터리 이름이며 한국 날짜로 9월 9일 작업이다. 이전 경로의 배포 스크립트를 다시 실행하지 않는다. 사전 빌드 배포에는 RELEASE_COMPOSE_FILE, RELEASE_PROJECT_NAME, RELEASE_PREBUILT_BUNDLE을 현재 경로에 맞게 설정해야 한다.

## 검증

- 확정 소스의 백엔드 171개, Linux 배포 회귀 28개, 홈·테마·날짜 JS 18개 통과.
- Linux/AMD64 두 이미지 빌드, 격리된 통합 배포 리허설 통과.
- 운영 이미지·소스 manifest 일치, OOM 없음, 서비스 running 확인.
- 백업 SHA256SUMS 전부 일치. 비공개 및 공개 점검 각각 통과.
- TLS, 정적 자산, 메인·블로그 API, robots/sitemap, www 리다이렉트 확인.
- 운영 브라우저에서 375/1280px × light/dark, 제목/정렬 분리, 좌우 구도, 최신·인기 전환, 원문 링크, 블로그 로딩 확인. 인증서 우회나 API 응답 모킹 없음.
- 블로그 소유자 1계정만 작성 가능함을 서버에서 재확인. 실제 OAuth 로그인 완료를 대신하는 검사는 아니다.

기존 Signal 상태 수는 공개 6·검토 2·보관 6으로 유지됐다. 클로드 해설은 운영 DB에 추가하지 않았다. 수집기 토큰은 비활성 상태를 유지한다.

## 공간 정리

section-lines-20260908/images.tar 및 theme-label-20260908/images.tar의 이전 전송 사본만 삭제해 387,341,824바이트를 확보했다. 로컬 원본과 SHA-256 일치 및 서버 로드 이미지 존재를 확인한 뒤 삭제했다. DB 백업·복구 이미지·소스는 보존했고 전송 사본은 로컬에서 다시 전송할 수 있다. 배포 후 확인한 서버 여유는 2,321,305,600바이트였다.

## GitHub 동기화 범위와 남은 항목

GitHub에는 위 배포 manifest에 포함된 소스 및 관련 공개 문서·테스트를 동기화한다. 미발행 원고(docs/drafts), Docling 예제 원고, 환경변수·키·로컬 증거·전송 파일은 제외한다. 배포에 포함되지 않은 CI 확장안 및 자동 수집 워크플로 변경은 이번 업로드에 섞지 않는다.

블로그 전체 lint의 기존 set-state-in-effect 오류 1건, 실제 Google 로그인 완료, 운영 규모의 순위 집계 성능 확인은 남아 있다. 이를 통과했다고 주장하지 않는다. GitHub 업로드는 추가 운영 배포나 수집 활성화를 의미하지 않는다.
